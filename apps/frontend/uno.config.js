// uno.config.ts
import { defineConfig } from 'unocss'

export default defineConfig({
  // optional safelist for classes you use dynamically
  safelist: [],
  theme: {
    // your custom theme settings if needed
  },
  rules: [],
})